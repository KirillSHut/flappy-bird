import { TEColorsHexadecimal } from "../types";

export interface IResultsBarGraphicsParams {
    x: number,
    y: number,
    width: number,
    height: number,
    color: TEColorsHexadecimal,
    opacity: number
}